<%- include('partials/header') %> 
<main>
  <section class="carrinho-header">
    <h3>Carrinho</h3>
  </section>

  <section class="container">
    <% produtos.forEach(produto => { %>
     <div class="row">
      <div class="col">
        <%= produto.nome %> 
      </div>
      <div class="col">
        <%= produto.preco %>
      </div>
      <div class="col">
        <button>Atualizar</button>
        <form action="/carrinho/delete/<%= produto.id %>?_method=DELETE" method="post">
          <button>Remover</button>
        </form>
      </div>
     </div>
    <% }) %>
  </section>
</main>
<%- include('partials/footer') %> 
