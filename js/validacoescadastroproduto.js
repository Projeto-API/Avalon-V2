window.addEventListener('load', () => {
    const tituloLivro = document.getElementById('titulo')
    const categoria = document.getElementById('categoria')
  

   formulario.addEventListener('submit', function (event){

    if (Input.value.length > 0 && senhaInput.value.length > 0) {
        alert (msgSucesso);
    } else {
        console.log('bagaça');
    }
})
})
